fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
python-dotenv==1.0.1
aiofiles==24.1.0
reportlab==4.2.4
Pillow==10.4.0
httpx==0.27.2
python-jose[cryptography]==3.3.0
passlib>=1.7.4
argon2-cffi>=23.1.0
jinja2==3.1.4
psycopg2-binary==2.9.9
cloudinary==1.40.0

# OAuth2 Authorization Server — Sprint 7
cryptography>=43.0.0            # RSA key generation (transitive of python-jose[cryptography], made explicit)
authlib>=1.3.1                  # optional: used for advanced client-side OAuth utilities

# Webhook Delivery Engine — Sprint 7
celery[redis]>=5.4.0            # Task queue for async webhook delivery
redis>=5.0.0                    # Celery broker + result backend
kombu>=5.3.0                    # Celery messaging library (transitive, made explicit)

# Push Notifications — Sprint 6
# firebase-admin==6.5.0       # Uncomment when GOOGLE_APPLICATION_CREDENTIALS is set
# PyJWT==2.8.0                # APNs JWT signing (ES256)
# cryptography==43.0.0        # Required by PyJWT for EC keys
# h2==4.1.0                   # HTTP/2 support for APNs via httpx

# SSO / SAML 2.0 / OIDC / MFA / SCIM 2.0 — Sprint 8
python3-saml==1.16.0           # SAML 2.0 Service Provider (python3-saml)
pyotp==2.9.0                   # TOTP (RFC 6238) — Google Authenticator compatible
qrcode[pil]==7.4.2             # QR code generation for TOTP enrollment
webauthn==2.1.0                # WebAuthn / FIDO2 / Passkeys (FIDO2 level 2)
cryptography>=43.0.0           # Fernet symmetric encryption for TOTP secrets (already transitive)

# Enterprise Billing — Sprint 8
# stripe>=10.0.0               # Official Stripe SDK (optional; we use raw httpx for full control)
sendgrid>=6.11.0               # Transactional email (dunning, invoice delivery, trial reminders)
